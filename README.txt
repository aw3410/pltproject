Teammates: Annika Wang (aw3410) and Kira Ariyan (kna2121)

1. In generator.py, we created an algorithm that processes an input AST 
    for the fairytale language and outputs Python. By translating our high level language 
    to another high level language, we create a program that can then be run to produce 
    the output that our fairytale language would be expected to produce.

2. In programs 3 and 4, we demonstrated programs that intentionally have syntax or 
    semantic errors to validate error reporting and handling. 
   Programs 1, 2, and 5 are sample programs cover a wide range of language features including
   loops, if statements, assignments and expressions.  Our sample programs incorporate each part of our compiler,
   converting code from our language to a token list using the scanner, then an abstract syntax tree using our 
   parser, and finally generating executable code using our code generator algorithm.

3. Our shell script calls upon each of our program files which call the scanner, parser and generator, and output 
the result to an output file which then runs the generated code to display the expected output.


6. Video: 